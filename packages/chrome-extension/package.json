{
  "name": "spectre-chrome-extension",
  "version": "0.1.0",
  "description": "Spectre AI — Crypto Intelligence for X (Twitter)",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
