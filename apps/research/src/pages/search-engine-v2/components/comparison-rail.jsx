/**
 * ComparisonRail — winner score card for COMPARISON queries.
 * v2.1 stub. Renders nothing for now so the layout reads as full-width.
 */
export default function ComparisonRail() { return null }
