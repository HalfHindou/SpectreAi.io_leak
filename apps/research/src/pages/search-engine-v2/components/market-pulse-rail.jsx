/**
 * market-pulse-rail — v2.1 stub. Returns null so MACRO/MARKET_OVERVIEW render
 * full-width main column. The classification-config still points here so the
 * upgrade in v2.1 doesn't need a config change.
 */
export default function MarketPulseRail() { return null }
