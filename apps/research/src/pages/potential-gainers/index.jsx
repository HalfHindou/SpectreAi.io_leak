/**
 * Potential Gainers - premium X Dash leaderboard.
 *
 * Thin page wrapper. All UI + state lives in components/. Standalone route
 * registered in App.jsx at /potential-gainers.
 */
import PotentialGainersPage from './components/potential-gainers-page'

export default function PotentialGainers() {
  return <PotentialGainersPage />
}
